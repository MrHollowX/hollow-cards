# Shared coding-agent instructions

This repository uses one agent-neutral project skill. Read
[`ai-skill/SKILL.md`](ai-skill/SKILL.md) before changing the custom cards,
tests, documentation, or their Home Assistant integration workflow.

The skill is intentionally stored in the repository so any coding agent can
consume the same source instead of relying on tool-specific copies.
